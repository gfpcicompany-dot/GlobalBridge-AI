# ── Authentication ──
JWT_SECRET=globalbridge-dev-secret-2026

# ── Database (optional - app works without it but data won't persist) ──
# DATABASE_URL=mysql://root:password@localhost:3306/globalbridge

# ── AI / LLM (optional - AI features only) ──
# BUILT_IN_FORGE_API_URL=https://your-llm-api.com
# BUILT_IN_FORGE_API_KEY=your_api_key_here
